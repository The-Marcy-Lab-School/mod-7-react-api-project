import { Route, Switch } from "react-router-dom";
import React from "react";

//*import the home page once created 
//*import routes into the app also as a return routes inside of router
//*also import router dom into the app

const Routes = () => {
    <Switch>
        <Route>
 
        <Route path='/form' element={<Form/>} />

        </Route>
    </Switch>
}

export default Routes
